[package]
name = "pce-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "pce"
path = "src/lib.rs"

[features]
default = []
runtime-debug-flags = []
trace_hw_writes = ["runtime-debug-flags"]

[dependencies]
bincode = { version = "2.0.1", features = ["derive"] }
